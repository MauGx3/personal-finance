apiVersion: v1
kind: Service
metadata:
  name: personal-finance-pg
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: personal-finance
    component: postgresql
  clusterIP: None
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: personal-finance-pg
spec:
  serviceName: personal-finance-pg
  replicas: 1
  selector:
    matchLabels:
      app: personal-finance
      component: postgresql
  template:
    metadata:
      labels:
        app: personal-finance
        component: postgresql
    spec:
      containers:
        - name: postgresql
          image: postgres:17-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: personal_finance_db
            - name: POSTGRES_USER
              value: personal_finance_user
            - name: POSTGRES_PASSWORD
              value: changeme
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: postgresql-data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
  volumeClaimTemplates:
    - metadata:
        name: postgresql-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
