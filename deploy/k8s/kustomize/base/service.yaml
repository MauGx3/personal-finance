apiVersion: v1
kind: Service
metadata:
  name: personal-finance-web
  labels:
    app: personal-finance
    component: web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: personal-finance
    component: web
