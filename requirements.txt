# Base requirements for CI/CD and production deployment
# This file should be used for automated deployments and CI builds.
# For local development, use: pip install -r requirements/local.txt
# For production deployment, use: pip install -r requirements/production.txt

# Install base dependencies from the main requirements structure
-r requirements/base.txt

# Additional packages for CI/CD environments
pip-audit>=2.9.0

# Security-pinned indirect dependencies (pinned by Snyk to avoid vulnerabilities)
setuptools>=78.1.1
aiohttp>=3.12.14
anyio>=4.4.0
dnspython>=2.6.1
fonttools>=4.43.0
h11>=0.16.0
protobuf>=4.25.8
werkzeug>=3.0.6
zipp>=3.19.1
sqlparse>=0.5.0
